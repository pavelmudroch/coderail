# Loops

Prefer `for...of` and `for` loops for complex or multi-step transformations.
Use `map`, `filter`, and `reduce` for simple, single-purpose operations.
Avoid deeply chaining functional methods when it reduces readability.
Consider readability vs performance trade-offs when choosing loop constructs.